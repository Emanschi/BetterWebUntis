/**
 * Platzhalter-Shell. Routing, Theme und Screens folgen in M4 (siehe PLAN.md).
 */
export function App() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-lg flex-col justify-center gap-3 p-6">
      <h1 className="text-2xl font-semibold tracking-tight">BetterWebUntis</h1>
      <p className="text-sm opacity-70">
        API-Layer steht (M2). App-Shell, Theme und Screens folgen in M4.
      </p>
    </main>
  );
}
