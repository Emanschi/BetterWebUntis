import { StubScreen } from './StubScreen';

export function AppointmentsScreen() {
  return (
    <StubScreen
      title="Meine Termine"
      milestone="M7"
      description="Annäherung an Sprechstunden/Bereitschaft über lstype 'oh'/'sb' im Stundenplan und getSubstitutions — die API hat dafür keine eigene Methode (siehe IDEEN.md A4)."
    />
  );
}
